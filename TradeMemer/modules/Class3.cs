using System;
using System.Collections.Generic;
using System.Text;
using Discord;
using Public_Bot;
using System.Threading.Tasks;

namespace TradeMemer.modules
{
    /*
    [DiscordCommandClass("test","test")]
    class testClass: CommandModuleBase
    {
        [DiscordCommand("plus")]
        public async Task Addition(int x, int y)
        {
            var sum = x + y;
            await ReplyAsync("The sum is " + sum);
        }



        [DiscordCommand("about")]
        public async Task AboutUs(params string[] _)
        {
            var x = new EmbedBuilder();
            x.Title = "About our Developers";
            x.Description = "DJ001#0915 is C#, Py coder and _chedan is C#";
            x.WithCurrentTimestamp();
            var finalEmbed = x.Build();
            await ReplyAsync(embed: finalEmbed);
        }
    }*/
}
